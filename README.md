# Automobile Service Request & Invoice API

This project implements a FastAPI server with Pydantic validation, SQLAlchemy ORM, and PostgreSQL persistence for:
- Service Requests (`POST /Service_Request/`)
- Invoices (`POST /Invoice/`)

Follow instructions in the README to run locally. The project was generated for the user; update `.env` with your DATABASE_URL and secrets (already provided in .env here).

